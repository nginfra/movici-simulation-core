{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "/data_collector/1.0.0",
  "additionalProperties": false,
  "properties": {
    "gather_filter": {
      "oneOf": [
        {
          "type": "object"
        },
        {
          "type": "null"
        },
        {
          "const": "*"
        }
      ]
    },
    "aggregate_updates": {
      "type": "boolean"
    },
    "storage": {
      "type": "string",
      "enum": ["api", "file", "sqlite"],
      "description": "Override the default storage strategy for this data_collector instance. The model may fail if the specified strategy is not available"
    },
    "storage_dir": {
      "type": "string",
      "description": "Directory for file-based storage or fallback for SQLite database path"
    },
    "database_path": {
      "type": "string",
      "description": "Path to SQLite database file (for sqlite backend). If not specified, uses storage_dir/simulation_results_<timestamp>.db. Setting this will force the data collector to use sqlite storage"
    }
  }
}
